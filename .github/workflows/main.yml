name: Auto Deploy
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 使用别人封装好的的action，用于clone该仓库的源码到工作流中
      - name: Checkout
        uses: actions/checkout@v3

      # 在工作流中安装node环境
      # - name: Setup node
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: 16

      # 打包代码生成环境
      # - name: Build
      #   run: |
      #     cd client # 进入前端侧的工程目录
      #     npm install # 安装依赖
      #     npm run build # 打包前端代码

      # 同步代码到服务器（目标路径：/home/ubuntu/mern-project/ecommerce-mern）
      # - name: Deploy
      #   uses: cross-the-world/scp-pipeline@master
      #   with:
      #     host: ${{ secrets.MY_HOST }} # 服务器IP（需要在GitHub上自行配置对应的secret）
      #     user: ${{ secrets.MY_USER }} # 服务器用户名
      #     pass: ${{ secrets.MY_PASS }} # 服务器密码
      #     connect_timeout: 10s
      #     local: './' # 源路径（工作流）
      #     remote: /home/ubuntu/mern-project/ecommerce-mern # 目标路径（服务器）

      # 在服务器端执行相关指令
      - name: Executing remote ssh commands
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.MY_HOST }} # 服务器IP（需要在GitHub上自行配置对应的secret）
          username: ${{ secrets.MY_USER }} # 服务器用户名
          password: ${{ secrets.MY_PASS }} # 服务器密码
          script: |
            cd /home/ubuntu/mern-project/ecommerce-mern
            git pull
            docker-compose up -d
